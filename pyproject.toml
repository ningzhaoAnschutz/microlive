[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "microlive"
dynamic = ["version"]
description = "Live-cell microscopy image analysis and single-molecule measurements"
readme = "README.md"
license = "GPL-3.0-or-later"
requires-python = ">=3.10"
authors = [
    { name = "Luis U. Aguilera" },
    { name = "William S. Raymond" },
    { name = "Rhiannon M. Sears" },
    { name = "Nathan L. Nowling" },
    { name = "Brian Munsky" },
    { name = "Ning Zhao" },
]
keywords = [
    "microscopy",
    "live-cell",
    "single-molecule",
    "tracking",
    "PyQt5",
    "cellpose",
    "particle-tracking",
    "image-analysis",
    "bioimaging",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: X11 Applications :: Qt",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: OS Independent",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
    "Topic :: Scientific/Engineering :: Image Processing",
    "Topic :: Scientific/Engineering :: Visualization",
]

dependencies = [
    # Core scientific stack - EXACT versions to prevent resolution loops
    "numpy==1.26.4",
    "pandas==2.2.2",
    "scipy==1.13.1",
    "scikit-image==0.24.0",
    "scikit-learn==1.5.2",
    "matplotlib==3.9.2",
    "matplotlib-scalebar",
    "seaborn==0.13.2",
    "statsmodels==0.14.4",
    
    # Image I/O
    "tifffile>=2024.1",
    "imagecodecs",
    "imageio>=2.34",
    "Pillow>=10.0",
    "bioio==3.1.0",
    "bioio-base==3.1.0",
    "bioio-lif>=1.2",
    "readlif==0.6.5",
    "opencv-python-headless>=4.9",
    "roifile",
    
    # GUI
    "PyQt5>=5.15",
    
    # ML/Analysis - Cellpose pulls PyTorch automatically
    "cellpose==3.1.0",
    "torchvision>=0.16",  # Required for ML spot detection (TrueSpot CNN)
    "trackpy>=0.6",
    "big-fish>=0.6",
    
    # Acceleration - EXACT versions to prevent numba/llvmlite conflicts
    "numba==0.60.0",
    "llvmlite==0.43.0",
    "joblib>=1.3",
    "dask==2024.8.0",
    "distributed==2024.8.0",
    
    # Utilities
    "tqdm>=4.66",
    "pystackreg>=0.2.8",
    "openpyxl>=3.1",
    "PyYAML>=6.0",
    "pydantic>=2.0",
    "natsort",
    "psutil",
    "ipython",
    "ipywidgets",
    
    # Bio utilities
    "biopython>=1.81",
    "dna-features-viewer>=3.1",
    "snapgene-reader>=0.1",
    "fpdf>=1.7",
    
    # Plotting extras
    "brokenaxes",
    "joypy",
    
    # TASEP modeling
    "tasep-models",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-qt>=4.0",
    "black>=23.0",
    "isort>=5.12",
    "mypy>=1.5",
    "ruff>=0.1",
    "build",
    "twine",
    "pdoc",
]
notebooks = [
    "jupyter>=1.0",
    "ipywidgets>=8.0",
    "ipympl>=0.9",
    "bqplot",
]

[project.scripts]
microlive = "microlive.gui.main:main"

[project.urls]
Homepage = "https://github.com/ningzhaoAnschutz/microlive"
Documentation = "https://github.com/ningzhaoAnschutz/microlive/blob/main/docs/user_guide.md"
Repository = "https://github.com/ningzhaoAnschutz/microlive"
Issues = "https://github.com/ningzhaoAnschutz/microlive/issues"
Changelog = "https://github.com/ningzhaoAnschutz/microlive/releases"

# =============================================================================
# Build Configuration
# =============================================================================

[tool.hatch.version]
path = "microlive/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["microlive"]
# Force include non-Python data files (ML models)
exclude = [
    "*.log",
    "**/micro_gui.log",
    "**/__pycache__",
    "**/*.pyc",
]

[tool.hatch.build.targets.wheel.force-include]
"microlive/data/models/spot_detection_cnn.pth" = "microlive/data/models/spot_detection_cnn.pth"

[tool.hatch.build.targets.sdist]
include = [
    "/microlive",
    "/README.md",
    "/LICENSE",
]
exclude = [
    # Exclude development/research files from source distribution
    "/modeling",
    "/notebooks",
    "/installation",
    "/scripts",
    "/tests",
    "/temp",
    "/test_gui",
    "/writing",
    "/.gemini",
    "/.git",
    "/.github",
    "/.backup_before_pip_packaging",
    "__pycache__",
    "*.pyc",
    "*.log",
]

[tool.hatch.build.targets.sdist.force-include]
"microlive/data/models/spot_detection_cnn.pth" = "microlive/data/models/spot_detection_cnn.pth"

# =============================================================================
# Tool Configuration
# =============================================================================

[tool.black]
line-length = 100
target-version = ["py310", "py311", "py312"]
exclude = '''
/(
    \.git
    | \.hg
    | \.mypy_cache
    | \.tox
    | \.venv
    | _build
    | buck-out
    | build
    | dist
)/
'''

[tool.isort]
profile = "black"
line_length = 100
skip_gitignore = true

[tool.ruff]
line-length = 100
target-version = "py310"
exclude = [
    ".git",
    ".mypy_cache",
    ".ruff_cache",
    ".venv",
    "build",
    "dist",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
]
ignore = [
    "E501",  # line too long (handled by black)
    "B008",  # do not perform function calls in argument defaults
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
